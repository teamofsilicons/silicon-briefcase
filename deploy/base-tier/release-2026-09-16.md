# Briefcase Honeycomb production deployment — 2026-09-16

Deployed application `tos>briefcase`, version `1.1.0`, to the existing base-tier ARM64 host `i-00c2b0c4968b57186` in `us-east-1`. No infrastructure topology or DNS changes were made.

## Source and database integrity

The checkout was advanced to deployed production revision `115bc151e57049a538ffccc44bfac37c8272db44`, preserving user edits and the Honeycomb implementation. This retained the existing imported data-organization, delegated scope, optional tag, and recipient fixes. Canonical migration 38 was preserved; the new Honeycomb migrations are 39–44. Migration 42 preserves the data-organization routing rule while adding the Honeycomb credential fence.

Before rollout, all 38 deployed migration checksums matched source. Both databases were backed up with verified custom-format PostgreSQL dumps. Both now report 44 successful migrations. Fresh disposable PostgreSQL databases also passed the complete 256-test backend suite; one existing live telemetry test was ignored. Strict Clippy and advisory checks passed. The Rust client/CLI suite previously passed 134 tests, and six final CLI binaries were rebuilt with the merged manuals.

## Runtime rollout

- API and worker image: `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-briefcase-production@sha256:749015317920ba0b9c1fa5b806e09fb3e7f5a13d72db4d214b60944a4666f2da`.
- Browser image: `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-briefcase-production@sha256:c85fb615b266ced0f9c29af19de8fe6fd92b36e85f3f0834e7f61127f5406d65`.
- Final documentation image: `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-briefcase-production@sha256:56f8ff5936e56bd2a4c9f1ba23c76f633340ce93382788cdbfdf9c866f184e1e`; content release `/var/www/briefcase-docs/releases/honeycomb-20260916-final`.
- Final docs-only SSM rollout: `714424fb-175f-4637-b796-5863b0d9bcda`, succeeded; final workflow page and its Honeycomb target returned HTTPS 200.
- Main SSM rollout: `e2f0023f-9955-4db7-85b3-8883ac16ed19`, succeeded.
- Production and testing database backups, previous units, env files, image references, and docs link: `/var/lib/silicon-briefcase/releases/honeycomb-20260916/` on the host. The directory is private. Temporary migrator credentials were removed after rollout.

The supplied app secret was matched to the fresh IAM registration, and its corresponding webhook signing credential was installed. Existing database passwords, encryption keys, and unrelated settings were preserved. A dedicated service credential was stored in both service secret stores and loaded internally. Honeycomb deployment tooling provisions it automatically; users do not generate or copy a testing service token.

The coordinated Honeycomb task deployed its Briefcase lifecycle sender using backend image `234951665042.dkr.ecr.us-east-2.amazonaws.com/silicon-honeycomb-backend@sha256:2234baa2e5e367edfed3947b8b7a0f3bb2aa87b8544c56df48a7565b034e80ff`, SSM `7c6e602f-6f1b-4092-948e-21d574a728c2`. Its frontend and proxy containers were preserved.

## Live verification

- Backend readiness, browser, documentation, OpenAPI, and the Honeycomb integration page returned HTTPS 200.
- Browser and documentation responses matched the built artifacts byte for byte.
- Anonymous protected receipt lookup returned 401. The dedicated service credential reached receipt lookup and returned the expected 404 for a random missing operation, without mutating any environment.
- API, worker, browser, and Nginx were active with the expected images.
- A later stability check confirmed zero restarts for all three Briefcase containers.
- An initial docs directory traversal permission error was detected by public verification and corrected to mode 0755; subsequent HTTPS and content checks passed.

## Final CLI archive

`dist/briefcase-1.1.0.tar.gz` contains all six native targets, is 20,715,240 bytes, and passed Honeycomb validation after the final manual rebuild.

SHA-256: `a2a1fce14171d3b03f0c8a5942f17c69079b15ebad9b783d680e0ce05b85ccb0`.

## Remaining limits and rollback

This deployment enables Briefcase's participant transport. Shared test-environment creation still requires the unimplemented IAM lifecycle transport; it is not established by these checks. Authenticated user upload/read and live S3 acceptance were not repeated. Existing dependency license/source/wildcard policy failures and existing frontend dependency findings remain recorded in the build report.

For an application rollback, restore the prior units and runtime env files from the private host backup and restart the changed services; restore the previous docs symlink separately. Keep forward database migrations in place. Do not overwrite database backups or replace encryption keys. The rollout report and verification evidence contain no credential values.

## Configured identity and publication-readiness follow-up

The final running API/worker image is `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-briefcase-production@sha256:e6af18f1d4e7b84b2862320d6fd0bfaf23bf8a656e0859e4771e1a269e1744d5` (source SHA-256 `a30176a4fd96ceab4353543cb5fe8512b2641acd979a4549eae7c0f55a5583f4`). It derives participant identity from `BRIEFCASE_IAM_APP_ID` for lifecycle requests, selected retirement, receipts, and activity reporting. No fixed application handle grants cross-service access. Wrong-app requests fail before database work, and receipts/activity cannot be reused under a different configured identity. Tests cover an unrelated `acme>storage` identity.

SSM `3b15cf17-19d0-469d-960a-5b12f5b89339` completed successfully. Both services run the expected image with zero restarts; the browser container was preserved. The backend suite again passed 256 tests and strict Clippy. This follow-up required no new migration. Rollback units and the previous docs symlink are retained at `/var/lib/silicon-briefcase/releases/honeycomb-20260916-configured-identity/`.

Final documentation image: `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-briefcase-production@sha256:fd92b86084975eb70d13b2a20e5c220245dc33aca58cd637e1f6d1814df95398`. Its current content release is `/var/www/briefcase-docs/releases/honeycomb-20260916-configured-identity`. Public backend/web/docs returned HTTPS 200, the integration page matched its packaged bytes, dedicated authentication passed, and an authenticated wrong-app request returned 422 without creating an environment.

SSM `c39e7354-778f-442e-a923-6796a96b0ebd` verified live storage readiness. The organization has no custom bucket override or pending delegated uploads and has ample quota. A random, isolated probe object in its exact platform storage prefix was created, read back byte-for-byte, and removed using the runtime instance identity. No package archive or application file was uploaded by this probe.

The final archive above is unchanged. Its native executable passed production contract/IAM discovery and clean login-state checks in an isolated Silicon home. Actual archive upload, public-link activation, and package publication are coordinated by the Honeycomb task, which was notified that Briefcase is ready.
